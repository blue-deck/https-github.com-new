# Disallow transitive Git dependencies; reviewed root dependencies may opt in.
allow-git=root

# Keep installs tied to the reviewed lockfile and audit advisories by default.
package-lock=true
audit=true
